name: First Workflow

on: push

jobs:
    firstJob:
        runs-on: ubuntu-latest
        steps:
            - name: Greeting
              run: echo "Hello!!!"
            - name: OS Name
              run: uname -n
            - name: Disk Free
              run: df -h
